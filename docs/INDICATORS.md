# TAFlow indicator reference

**393** classes — **161** with a TA-Lib equivalent, **232** extended operators with no TA-Lib counterpart.

> Generated by `scripts/gen_indicators_doc.py` from the installed package. Do not edit by hand.

Classes are sorted alphabetically by their full canonical names. Python signatures are introspected from the installed adapters; Rust signatures come from each canonical implementation file. Python defaults are shown verbatim. Rust has no default arguments, so every Rust constructor parameter is explicit.

## Python usage

Every class behaves the same way:

```python
from taflow import SimpleMovingAverage

ind = SimpleMovingAverage(timeperiod=30)         # configuration only
ind.extend(close)                                # historical backfill
ind.append(float(close[-1]))                     # O(1) live update
ind.value                                        # latest value, None during warm-up
ind.compute()                                    # full aligned series
len(ind)                                         # bars consumed
ind.reset()                                      # clear state and history in place
```

Outputs are `float64` arrays the same length as the input, `NaN` through
warm-up. Multi-output indicators return a tuple. Candle patterns return
`int32` scores (`0`, `±100`).

## Rust usage

```rust
use taflow::indicators::SimpleMovingAverage;
use taflow::stream::StreamingIndicator;

fn calculate_simple_moving_average(
    close: &[f64],
) -> taflow::TaResult<Vec<f64>> {
    let mut simple_moving_average = SimpleMovingAverage::new(30)?;
    let mut output = Vec::with_capacity(close.len());
    simple_moving_average.extend_slice_into(close, &mut output);
    Ok(output)
}
```

Rust constructors require every configuration parameter. Single-input classes
implement `StreamingIndicator`; multi-input and multi-output classes expose
equivalent inherent `append`, `value`, `reset`, and bulk methods.

## Parameter reference contract

Every constructor accepts configuration only. Historical series are
passed to ``extend`` in the documented input order. Configuration values have
defaults unless the algorithm cannot define one semantically:

```python
from taflow import SimpleMovingAverage, MoneyFlowIndex

SimpleMovingAverage(timeperiod=30).extend(close)
MoneyFlowIndex(timeperiod=14).extend(high, low, close, volume)
```

The Python constructor, `append`, and `extend` columns contain the complete public
parameter lists, including annotations, keyword-only markers, and default
values. The Rust constructor and `append` columns contain their exact explicit
parameters. Passing Python data by keyword always works.

## Alphabetical class reference

| Class | External name | Input order | Python constructor | Python `append` | Python `extend` | Rust constructor | Rust `append` |
|---|---|---|---|---|---|---|---|
| `AbsoluteBreadthIndex` | — | `(advancers, decliners)` | `()` | `(advancers: float, decliners: float)` | `(advancers: Any, decliners: Any)` | `new()` | `append(&mut self, advancers: f64, decliners: f64)` |
| `AbsolutePriceOscillator` | APO | `(_input)` | `(fastperiod: int = 12, slowperiod: int = 26, moving_average_type: int = 0)` | `(_input: float)` | `(_input: Any)` | `new(fast_period: usize, slow_period: usize, ma_type: MaType)` | `append(&mut self, input: f64)` |
| `AccelerationBands` | ACCBANDS | `(high, low, close)` | `(period: int = 20)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `AccumulationDistribution` | AD | `(high, low, close, volume)` | `()` | `(high: float, low: float, close: float, volume: float)` | `(high: Any, low: Any, close: Any, volume: Any)` | `new()` | `append(&mut self, high: f64, low: f64, close: f64, volume: f64)` |
| `AccumulationDistributionOscillator` | ADOSC | `(high, low, close, volume)` | `(fastperiod: int = 3, slowperiod: int = 10)` | `(high: float, low: float, close: float, volume: float)` | `(high: Any, low: Any, close: Any, volume: Any)` | `new(fast_period: usize, slow_period: usize)` | `append(&mut self, high: f64, low: f64, close: f64, volume: f64)` |
| `AdaptiveCycle` | — | `(prices)` | `()` | `(price: float)` | `(prices: Any)` | `new()` | `append(&mut self, input: f64)` |
| `Amihud` | — | `(close, volume)` | `(timeperiod: int = 20)` | `(close: float, volume: float)` | `(close: Any, volume: Any)` | `new(timeperiod: usize)` | `append(&mut self, close: f64, volume: f64)` |
| `AnchoredVolumeWeightedAveragePrice` | — | `(high, low, close, volume, anchor)` | `(standard_deviation_multiplier: float = 1.0)` | `(high: float, low: float, close: float, volume: float, anchor: bool)` | `(high: Any, low: Any, close: Any, volume: Any, anchor: Any)` | `new(standard_deviation_multiplier: f64)` | `append(&mut self, high: f64, low: f64, close: f64, volume: f64, anchor: bool)` |
| `ArmsIndex` | — | `(advancers, decliners, advancing_volume, declining_volume)` | `()` | `(advancers: float, decliners: float, advancing_volume: float, declining_volume: float)` | `(advancers: Any, decliners: Any, advancing_volume: Any, declining_volume: Any)` | `new()` | `append(&mut self, advancers: f64, decliners: f64, advancing_volume: f64, declining_volume: f64)` |
| `ArnaudLegouxMovingAverage` | — | `(_input)` | `(timeperiod: int = 10, offset: float = 0.85, sigma: float = 6.0)` | `(_input: float)` | `(_input: Any)` | `new(period: usize, offset: f64, sigma: f64)` | `append(&mut self, input: f64)` |
| `Aroon` | AROON | `(high, low)` | `(timeperiod: int = 14)` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64)` |
| `AroonOscillator` | AROONOSC | `(high, low)` | `(timeperiod: int = 14)` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64)` |
| `AutomaticFibonacci` | — | `(high, low)` | `()` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new()` | `append(&mut self, high: f64, low: f64)` |
| `AverageDailyDollarValue` | — | `(close, volume)` | `(timeperiod: int = 20)` | `(close: float, volume: float)` | `(close: Any, volume: Any)` | `new(timeperiod: usize)` | `append(&mut self, close: f64, volume: f64)` |
| `AverageDailyRange` | — | `(open, high, low, close, volume, timestamp)` | `(period: int = 14, utc_offset_minutes: int = 0)` | `(open: float, high: float, low: float, close: float, volume: float, timestamp: int)` | `(open: Any, high: Any, low: Any, close: Any, volume: Any, timestamp: Any)` | `new(period: usize, utc_offset_minutes: i32)` | `append(&mut self, _open: f64, high: f64, low: f64, _close: f64, _volume: f64, timestamp: i64)` |
| `AverageDirectionalIndex` | ADX | `(high, low, close)` | `(period: int = 14)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `AverageDirectionalIndexRating` | ADXR | `(high, low, close)` | `(period: int = 14)` | `(high: object, low: object, close: object)` | `(high: object, low: object, close: object)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `AveragePrice` | AVGPRICE | `(open, high, low, close)` | `()` | `(open: float, high: float, low: float, close: float)` | `(open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `AverageTrueRange` | ATR | `(high, low, close)` | `(timeperiod: int = 14)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `AverageTrueRangeBands` | — | `(high, low, close)` | `(period: int = 14, multiplier: float = 3.0)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(period: usize, multiplier: f64)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `AwesomeOscillator` | — | `(high, low)` | `(fast: int = 5, slow: int = 34)` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new(fast: usize, slow: usize)` | `append(&mut self, high: f64, low: f64)` |
| `BalanceOfPower` | BOP | `(open, high, low, close)` | `()` | `(open: float, high: float, low: float, close: float)` | `(open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `BarsSince` | — | `(condition)` | `()` | `(condition: bool)` | `(condition: Any)` | `new()` | `append(&mut self, condition: bool)` |
| `BatPattern` | — | `(open, high, low, close)` | `()` | `(open: float, high: float, low: float, close: float)` | `(open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, _open: f64, high: f64, low: f64, _close: f64)` |
| `BetterVolume` | — | `(high, low, close, volume)` | `(period: int = 20)` | `(high: float, low: float, close: float, volume: float)` | `(high: Any, low: Any, close: Any, volume: Any)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64, _close: f64, volume: f64)` |
| `BollingerBands` | BBANDS | `(values)` | `(period: int = 5, deviations_up: float = 2.0, deviations_down: float = 2.0, moving_average_type: int = 0)` | `(value: float)` | `(values: Any)` | `new(period: usize, deviations_up: f64, deviations_down: f64, ma_type: MaType)` | `append(&mut self, input: f64)` |
| `BreadthThrust` | — | `(advancers, decliners)` | `(period: int = 10)` | `(advancers: float, decliners: float)` | `(advancers: Any, decliners: Any)` | `new(period: usize)` | `append(&mut self, advancers: f64, decliners: f64)` |
| `BreakOfStructureChangeOfCharacter` | — | `(high, low, close)` | `(swing_length: int = 5)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(swing_length: usize)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `BullishPercentIndex` | — | `(on_buy_signal_count, universe_size)` | `()` | `(on_buy_signal_count: float, universe_size: float)` | `(on_buy_signal_count: Any, universe_size: Any)` | `new()` | `append(&mut self, on_buy_signal_count: f64, universe_size: f64)` |
| `ButterflyPattern` | — | `(open, high, low, close)` | `()` | `(open: float, high: float, low: float, close: float)` | `(open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, _open: f64, high: f64, low: f64, _close: f64)` |
| `CandleAbandonedBaby` | CDLABANDONEDBABY | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleAdvanceBlock` | CDLADVANCEBLOCK | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleBeltHold` | CDLBELTHOLD | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleBreakaway` | CDLBREAKAWAY | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleClosingMarubozu` | CDLCLOSINGMARUBOZU | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `CandleConcealBabySwall` | CDLCONCEALBABYSWALL | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleCounterAttack` | CDLCOUNTERATTACK | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleDarkCloudCover` | CDLDARKCLOUDCOVER | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, _l: f64, c: f64)` |
| `CandleDoji` | CDLDOJI | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `CandleDojiStar` | CDLDOJISTAR | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleDragonflyDoji` | CDLDRAGONFLYDOJI | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `CandleEngulfing` | CDLENGULFING | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, _high: f64, _low: f64, close: f64)` |
| `CandleEveningDojiStar` | CDLEVENINGDOJISTAR | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleEveningStar` | CDLEVENINGSTAR | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, _h: f64, _l: f64, c: f64)` |
| `CandleGapSideSideWhite` | CDLGAPSIDESIDEWHITE | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleGravestoneDoji` | CDLGRAVESTONEDOJI | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `CandleHammer` | CDLHAMMER | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `CandleHangingMan` | CDLHANGINGMAN | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleHarami` | CDLHARAMI | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, _h: f64, _l: f64, c: f64)` |
| `CandleHaramiCross` | CDLHARAMICROSS | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleHighWave` | CDLHIGHWAVE | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `CandleHikkake` | CDLHIKKAKE | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, _open: f64, high: f64, low: f64, close: f64)` |
| `CandleHikkakeModified` | CDLHIKKAKEMOD | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, _open: f64, high: f64, low: f64, close: f64)` |
| `CandleHomingPigeon` | CDLHOMINGPIGEON | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, _h: f64, _l: f64, c: f64)` |
| `CandleIdenticalThreeCrows` | CDLIDENTICAL3CROWS | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleInNeck` | CDLINNECK | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleInvertedHammer` | CDLINVERTEDHAMMER | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleKicking` | CDLKICKING | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleKickingByLength` | CDLKICKINGBYLENGTH | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleLadderBottom` | CDLLADDERBOTTOM | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleLongLeggedDoji` | CDLLONGLEGGEDDOJI | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `CandleLongLine` | CDLLONGLINE | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `CandleMarubozu` | CDLMARUBOZU | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `CandleMatHold` | CDLMATHOLD | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, _l: f64, c: f64)` |
| `CandleMatchingLow` | CDLMATCHINGLOW | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleMorningDojiStar` | CDLMORNINGDOJISTAR | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleMorningStar` | CDLMORNINGSTAR | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, _h: f64, _l: f64, c: f64)` |
| `CandleOnNeck` | CDLONNECK | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandlePiercing` | CDLPIERCING | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, _h: f64, l: f64, c: f64)` |
| `CandleRickshawman` | CDLRICKSHAWMAN | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `CandleRiseFallThreeMethods` | CDLRISEFALL3METHODS | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleSeparatingLines` | CDLSEPARATINGLINES | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleShootingStar` | CDLSHOOTINGSTAR | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleShortLine` | CDLSHORTLINE | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleSpinningTop` | CDLSPINNINGTOP | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `CandleStalledPattern` | CDLSTALLEDPATTERN | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleStickSandwich` | CDLSTICKSANDWICH | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `CandleTakuri` | CDLTAKURI | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `CandleTasukiGap` | CDLTASUKIGAP | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleThreeBlackCrows` | CDL3BLACKCROWS | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `CandleThreeInside` | CDL3INSIDE | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, _high: f64, _low: f64, close: f64)` |
| `CandleThreeLineStrike` | CDL3LINESTRIKE | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `CandleThreeOutside` | CDL3OUTSIDE | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, _high: f64, _low: f64, close: f64)` |
| `CandleThreeStarsInSouth` | CDL3STARSINSOUTH | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `CandleThreeWhiteSoldiers` | CDL3WHITESOLDIERS | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleThrusting` | CDLTHRUSTING | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleTriStar` | CDLTRISTAR | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, h: f64, l: f64, c: f64)` |
| `CandleTwoCrows` | CDL2CROWS | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, _high: f64, _low: f64, close: f64)` |
| `CandleUniqueThreeRiver` | CDLUNIQUE3RIVER | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, _h: f64, l: f64, c: f64)` |
| `CandleUpDownSideGapThreeMethods` | CDLXSIDEGAP3METHODS | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, _high: f64, _low: f64, close: f64)` |
| `CandleUpsideGapTwoCrows` | CDLUPSIDEGAP2CROWS | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, o: f64, _h: f64, _l: f64, c: f64)` |
| `CenterOfGravity` | — | `(values)` | `(period: int = 10)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, x: f64)` |
| `ChaikinMoneyFlow` | — | `(high, low, close, volume)` | `(period: int = 20)` | `(high: float, low: float, close: float, volume: float)` | `(high: Any, low: Any, close: Any, volume: Any)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64, close: f64, volume: f64)` |
| `ChaikinVolatility` | — | `(high, low)` | `(timeperiod: int = 10, roc_period: int = 10)` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new(ema_period: usize, roc_period: usize)` | `append(&mut self, high: f64, low: f64)` |
| `ChandeMomentumOscillator` | CMO | `(values)` | `(timeperiod: int = 14)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `CloseToCloseSigma` | — | `(close)` | `(timeperiod: int = 20)` | `(close: float)` | `(close: Any)` | `new(timeperiod: usize)` | `append(&mut self, close: f64)` |
| `CommodityChannelIndex` | CCI | `(high, low, close)` | `(timeperiod: int = 14, *, high_column: str = 'high', low_column: str = 'low', close_column: str = 'close')` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any, *, high_column: str = 'high', low_column: str = 'low', close_column: str = 'close')` | `new(period: usize)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `CrabPattern` | — | `(open, high, low, close)` | `()` | `(open: float, high: float, low: float, close: float)` | `(open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, _open: f64, high: f64, low: f64, _close: f64)` |
| `Cross` | — | `(left, right)` | `()` | `(left: float, right: float)` | `(left: Any, right: Any)` | `new()` | `append(&mut self, left: f64, right: f64)` |
| `Crossover` | — | `(left, right)` | `()` | `(left: float, right: float)` | `(left: Any, right: Any)` | `new()` | `append(&mut self, left: f64, right: f64)` |
| `Crossunder` | — | `(left, right)` | `()` | `(left: float, right: float)` | `(left: Any, right: Any)` | `new()` | `append(&mut self, left: f64, right: f64)` |
| `CumulativeCount` | — | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, _input: f64)` |
| `CumulativeMaximum` | — | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `CumulativeMinimum` | — | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `CumulativeProduct` | — | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `CumulativeSum` | — | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `CumulativeSumControlChart` | — | `(change)` | `(threshold: float = 1.0)` | `(change: float)` | `(change: Any)` | `new(threshold: f64)` | `append(&mut self, change: f64)` |
| `CumulativeVolumeIndex` | — | `(advancing_volume, declining_volume)` | `()` | `(advancing_volume: float, declining_volume: float)` | `(advancing_volume: Any, declining_volume: Any)` | `new()` | `append(&mut self, advancing_volume: f64, declining_volume: f64)` |
| `CupAndHandle` | — | `(open, high, low, close)` | `()` | `(open: float, high: float, low: float, close: float)` | `(open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, _open: f64, high: f64, low: f64, _close: f64)` |
| `CypherPattern` | — | `(open, high, low, close)` | `()` | `(open: float, high: float, low: float, close: float)` | `(open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, _open: f64, high: f64, low: f64, _close: f64)` |
| `DayOfWeekReturnProfile` | — | `(open, high, low, close, volume, timestamp)` | `(utc_offset_minutes: int = 0)` | `(open: float, high: float, low: float, close: float, volume: float, timestamp: int)` | `(open: Any, high: Any, low: Any, close: Any, volume: Any, timestamp: Any)` | `new(utc_offset_minutes: i32)` | `append(&mut self, _open: f64, _high: f64, _low: f64, close: f64, _volume: f64, timestamp: i64)` |
| `DecayLinear` | — | `(input)` | `(timeperiod: int = 30)` | `(input: float)` | `(input: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `Decycler` | — | `(values)` | `(period: int = 20)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `DecyclerOscillator` | — | `(values)` | `(fast: int = 10, slow: int = 20)` | `(value: float)` | `(values: Any)` | `new(fast: usize, slow: usize)` | `append(&mut self, input: f64)` |
| `DemandIndex` | — | `(high, low, close, volume)` | `(timeperiod: int = 10)` | `(high: float, low: float, close: float, volume: float)` | `(high: Any, low: Any, close: Any, volume: Any)` | `new(timeperiod: usize)` | `append(&mut self, high: f64, low: f64, close: f64, volume: f64)` |
| `DetrendedPriceOscillator` | — | `(close)` | `(period: int = 20)` | `(close: float)` | `(close: Any)` | `new(period: usize)` | `append(&mut self, close: f64)` |
| `DirectionalMovementIndex` | DX | `(high, low, close)` | `(period: int = 14)` | `(high: object, low: object, close: object)` | `(high: object, low: object, close: object)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `Donchian` | — | `(high, low)` | `(timeperiod: int = 20)` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64)` |
| `DoubleBollingerBands` | — | `(values)` | `(period: int = 20, inner_multiplier: float = 1.0, outer_multiplier: float = 2.0)` | `(value: float)` | `(values: Any)` | `new(period: usize, inner_multiplier: f64, outer_multiplier: f64)` | `append(&mut self, input: f64)` |
| `DoubleExponentialMovingAverage` | DEMA | `(values)` | `(timeperiod: int = 30)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `Drawdown` | — | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `EaseOfMovement` | — | `(high, low, volume)` | `(period: int = 14, divisor: float = 100000000.0)` | `(high: float, low: float, volume: float)` | `(high: Any, low: Any, volume: Any)` | `new(period: usize, divisor: f64)` | `append(&mut self, high: f64, low: f64, volume: f64)` |
| `EhlersStochastic` | — | `(values)` | `(period: int = 10)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `EmpiricalModeDecomposition` | — | `(prices)` | `(period: int = 20, fraction: float = 0.5)` | `(price: float)` | `(prices: Any)` | `new(period: usize, fraction: f64)` | `append(&mut self, input: f64)` |
| `EntryExit` | — | `(entry, exit)` | `()` | `(entry: bool, exit: bool)` | `(entry: Any, exit: Any)` | `new()` | `append(&mut self, entry: bool, exit: bool)` |
| `EqualHighsLows` | — | `(high, low, close)` | `(eq_len: int = 3, atr_period: int = 200, eq_threshold: float = 0.1)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(eq_len: usize, atr_period: usize, eq_threshold: f64)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `EvenBetterSinewave` | — | `(close)` | `(length: int = 40)` | `(close: float)` | `(close: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `ExponentialMovingAverage` | EMA | `(values)` | `(timeperiod: int = 30)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `ExponentiallyWeightedCorrelation` | — | `(left, right)` | `(timeperiod: int = 14)` | `(left: float, right: float)` | `(left: Any, right: Any)` | `new(timeperiod: usize)` | `append(&mut self, left: f64, right: f64)` |
| `ExponentiallyWeightedCovariance` | — | `(left, right)` | `(timeperiod: int = 14)` | `(left: float, right: float)` | `(left: Any, right: Any)` | `new(timeperiod: usize)` | `append(&mut self, left: f64, right: f64)` |
| `ExponentiallyWeightedStandardDeviation` | — | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `ExponentiallyWeightedSum` | — | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `ExponentiallyWeightedVariance` | — | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `FairValueGap` | — | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `Falling` | — | `(_input)` | `(timeperiod: int = 1)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `FastStochasticOscillator` | STOCHF | `(high, low, close)` | `(fast_k_period: int = 5, fast_d_period: int = 3, fast_d_average_type: int = 0)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(fastk_period: usize, fastd_period: usize, fastd_matype: MaType)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `FibonacciArcs` | — | `(high, low)` | `()` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new()` | `append(&mut self, high: f64, low: f64)` |
| `FibonacciChannel` | — | `(high, low)` | `()` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new()` | `append(&mut self, high: f64, low: f64)` |
| `FibonacciConfluence` | — | `(high, low)` | `()` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new()` | `append(&mut self, high: f64, low: f64)` |
| `FibonacciExtension` | — | `(high, low)` | `()` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new()` | `append(&mut self, high: f64, low: f64)` |
| `FibonacciFan` | — | `(high, low)` | `()` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new()` | `append(&mut self, high: f64, low: f64)` |
| `FibonacciProjection` | — | `(high, low)` | `()` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new()` | `append(&mut self, high: f64, low: f64)` |
| `FibonacciRetracement` | — | `(close)` | `(window: int = 120)` | `(close: float)` | `(close: Any)` | `new(period: usize)` | `append(&mut self, close: f64)` |
| `FibonacciTimeZones` | — | `(high, low)` | `()` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new()` | `append(&mut self, high: f64, low: f64)` |
| `FisherTransform` | — | `(high, low)` | `(timeperiod: int = 10)` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new(timeperiod: usize)` | `append(&mut self, high: f64, low: f64)` |
| `FlagPennant` | — | `(open, high, low, close)` | `()` | `(open: float, high: float, low: float, close: float)` | `(open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, _open: f64, high: f64, low: f64, _close: f64)` |
| `ForceIndex` | — | `(close, volume)` | `(period: int = 13)` | `(close: float, volume: float)` | `(close: Any, volume: Any)` | `new(period: usize)` | `append(&mut self, close: f64, volume: f64)` |
| `FourPointHarmonicPattern` | — | `(open, high, low, close)` | `()` | `(open: float, high: float, low: float, close: float)` | `(open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, _open: f64, high: f64, low: f64, _close: f64)` |
| `FracDiff` | — | `(input)` | `(d: float = 0.5, threshold: float = 1e-05)` | `(input: float)` | `(input: Any)` | `new(d: f64, threshold: f64)` | `append(&mut self, input: f64)` |
| `FractalDimension` | — | `(prices)` | `(timeperiod: int = 20)` | `(price: float)` | `(prices: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `GapDown` | — | `(high, low)` | `()` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new()` | `append(&mut self, high: f64, low: f64)` |
| `GapUp` | — | `(high, low)` | `()` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new()` | `append(&mut self, high: f64, low: f64)` |
| `GarmanKlass` | — | `(_open, high, low, close)` | `(timeperiod: int = 20)` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new(timeperiod: usize)` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `GarmanKlassYangZhang` | — | `(_open, high, low, close)` | `(timeperiod: int = 20)` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new(timeperiod: usize)` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `GartleyPattern` | — | `(open, high, low, close)` | `()` | `(open: float, high: float, low: float, close: float)` | `(open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, _open: f64, high: f64, low: f64, _close: f64)` |
| `GoldenPocket` | — | `(high, low)` | `()` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new()` | `append(&mut self, high: f64, low: f64)` |
| `HeadAndShoulders` | — | `(open, high, low, close)` | `()` | `(open: float, high: float, low: float, close: float)` | `(open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, _open: f64, high: f64, low: f64, _close: f64)` |
| `HedgeRatio` | — | `(x, y)` | `(timeperiod: int = 20)` | `(x: float, y: float)` | `(x: Any, y: Any)` | `new(period: usize)` | `append(&mut self, x: f64, y: f64)` |
| `HeikinAshi` | — | `(_open, high, low, close)` | `()` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `HighLowIndex` | — | `(new_highs, new_lows)` | `(period: int = 10)` | `(new_highs: float, new_lows: float)` | `(new_highs: Any, new_lows: Any)` | `new(period: usize)` | `append(&mut self, new_highs: f64, new_lows: f64)` |
| `HigherHigh` | — | `(high, low)` | `()` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new()` | `append(&mut self, high: f64, low: f64)` |
| `HighestSince` | — | `(condition, _input)` | `()` | `(condition: bool, _input: float)` | `(condition: Any, _input: Any)` | `new()` | `append(&mut self, condition: bool, input: f64)` |
| `HilbertDominantCycle` | — | `(prices)` | `()` | `(price: float)` | `(prices: Any)` | `new()` | `append(&mut self, input: f64)` |
| `HilbertTransformDominantCyclePeriod` | HT_DCPERIOD | `(_input)` | `()` | `(value: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `HilbertTransformDominantCyclePhase` | HT_DCPHASE | `(_input)` | `()` | `(value: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `HilbertTransformPhasor` | HT_PHASOR | `(_input)` | `()` | `(value: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `HilbertTransformSineWave` | HT_SINE | `(_input)` | `()` | `(value: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `HilbertTransformTrendMode` | HT_TRENDMODE | `(_input)` | `()` | `(value: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `HilbertTransformTrendline` | HT_TRENDLINE | `(values)` | `()` | `(value: float)` | `(values: Any)` | `new()` | `append(&mut self, input: f64)` |
| `HullMovingAverage` | — | `(_input)` | `(timeperiod: int = 10)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `Hurst` | — | `(_input)` | `(timeperiod: int = 20, chunks: int = 4)` | `(_input: float)` | `(_input: Any)` | `new(period: usize, chunks: usize)` | `append(&mut self, input: f64)` |
| `HurstChannel` | — | `(high, low, close)` | `(period: int = 10, multiplier: float = 0.5)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(period: usize, multiplier: f64)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `Ichimoku` | — | `(high, low, close)` | `(tenkan: int = 9, kijun: int = 26, senkou: int = 52)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(tenkan: usize, kijun: usize, senkou: usize)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `InsideBar` | — | `(high, low)` | `()` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new()` | `append(&mut self, high: f64, low: f64)` |
| `InstantaneousTrendline` | — | `(values)` | `(period: int = 10)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `IntradayIntensity` | — | `(high, low, close, volume)` | `()` | `(high: float, low: float, close: float, volume: float)` | `(high: Any, low: Any, close: Any, volume: Any)` | `new()` | `append(&mut self, high: f64, low: f64, close: f64, volume: f64)` |
| `IntradayMomentumIndex` | IMI | `(open, close)` | `(timeperiod: int = 14)` | `(open: float, close: float)` | `(open: Any, close: Any)` | `new(period: usize)` | `append(&mut self, open: f64, close: f64)` |
| `IntradayVolatilityProfile` | — | `(open, high, low, close, volume, timestamp)` | `(buckets: int = 24, utc_offset_minutes: int = 0)` | `(open: float, high: float, low: float, close: float, volume: float, timestamp: int)` | `(open: Any, high: Any, low: Any, close: Any, volume: Any, timestamp: Any)` | `new(buckets: usize, utc_offset_minutes: i32)` | `append(&mut self, _open: f64, _high: f64, _low: f64, close: f64, _volume: f64, timestamp: i64)` |
| `InverseFisherTransform` | — | `(values)` | `(scale: float = 1.0)` | `(value: float)` | `(values: Any)` | `new(scale: f64)` | `append(&mut self, x: f64)` |
| `JurikMovingAverage` | — | `(values)` | `(length: int = 7, phase: float = 0.0)` | `(value: float)` | `(values: Any)` | `new(period: usize, phase: f64)` | `append(&mut self, input: f64)` |
| `KalmanHedgeRatio` | — | `(x, y)` | `(delta: float = 0.0001, observation_variance: float = 0.001)` | `(x: float, y: float)` | `(x: Any, y: Any)` | `new(delta: f64, observation_variance: f64)` | `append(&mut self, x: f64, y: f64)` |
| `KaufmanAdaptiveMovingAverage` | KAMA | `(values)` | `(timeperiod: int = 30)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `KeltnerChannels` | — | `(high, low, close)` | `(timeperiod: int = 20, multiplier: float = 2.0)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(period: usize, multiplier: f64)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `KlingerVolumeOscillator` | — | `(high, low, close, volume)` | `(fast: int = 34, slow: int = 55, signal: int = 13)` | `(high: float, low: float, close: float, volume: float)` | `(high: Any, low: Any, close: Any, volume: Any)` | `new(fast: usize, slow: usize, signal: usize)` | `append(&mut self, high: f64, low: f64, close: f64, volume: f64)` |
| `KnowSureThing` | — | `(close)` | `(roc1: int = 10, roc2: int = 15, roc3: int = 20, roc4: int = 30, sma1: int = 10, sma2: int = 10, sma3: int = 10, sma4: int = 15, signal: int = 9)` | `(close: float)` | `(close: Any)` | `new(roc1: usize, roc2: usize, roc3: usize, roc4: usize, sma1: usize, sma2: usize, sma3: usize, sma4: usize, nsig: usize)` | `append(&mut self, close: f64)` |
| `Lag` | — | `(_input)` | `(timeperiod: int = 1)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `LaguerreRelativeStrengthIndex` | — | `(close)` | `(gamma: float = 0.5)` | `(close: float)` | `(close: Any)` | `new(gamma: f64)` | `append(&mut self, input: f64)` |
| `LinearRegressionChannel` | — | `(values)` | `(period: int = 20, multiplier: float = 2.0)` | `(value: float)` | `(values: Any)` | `new(period: usize, multiplier: f64)` | `append(&mut self, input: f64)` |
| `Liquidity` | — | `(high, low)` | `(swing_length: int = 50, range_percent: float = 0.01)` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new(swing_length: usize, range_percent: f64)` | `append(&mut self, high: f64, low: f64, _close: f64)` |
| `LogReturn` | — | `(_input)` | `(timeperiod: int = 1)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `LowerLow` | — | `(high, low)` | `()` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new()` | `append(&mut self, high: f64, low: f64)` |
| `LowestSince` | — | `(condition, _input)` | `()` | `(condition: bool, _input: float)` | `(condition: Any, _input: Any)` | `new()` | `append(&mut self, condition: bool, input: f64)` |
| `MarketFacilitationIndex` | — | `(high, low, volume)` | `()` | `(high: float, low: float, volume: float)` | `(high: Any, low: Any, volume: Any)` | `new()` | `append(&mut self, h: f64, l: f64, v: f64)` |
| `MassIndex` | — | `(high, low)` | `(ema_period: int = 9, sum_period: int = 25)` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new(ema_period: usize, sum_period: usize)` | `append(&mut self, high: f64, low: f64)` |
| `MathAbs` | — | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathAcos` | ACOS | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathAcosh` | — | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathAdd` | ADD | `(left, right)` | `()` | `(left: float, right: float)` | `(left: Any, right: Any)` | `new()` | `append(&mut self, left: f64, right: f64)` |
| `MathAsin` | ASIN | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathAsinh` | — | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathAtan` | ATAN | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathAtanh` | — | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathCbrt` | — | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathCeil` | CEIL | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathCos` | COS | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathCosh` | COSH | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathCot` | — | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathDegrees` | — | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathDivide` | DIV | `(left, right)` | `()` | `(left: float, right: float)` | `(left: Any, right: Any)` | `new()` | `append(&mut self, left: f64, right: f64)` |
| `MathExp` | EXP | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathFloor` | FLOOR | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathLn` | LN | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathLog10` | LOG10 | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathLog1p` | — | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathMultiply` | MULT | `(left, right)` | `()` | `(left: float, right: float)` | `(left: Any, right: Any)` | `new()` | `append(&mut self, left: f64, right: f64)` |
| `MathRadians` | — | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathSin` | SIN | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathSinh` | SINH | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathSqrt` | SQRT | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathSubtract` | SUB | `(left, right)` | `()` | `(left: float, right: float)` | `(left: Any, right: Any)` | `new()` | `append(&mut self, left: f64, right: f64)` |
| `MathTan` | TAN | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `MathTanh` | TANH | `(_input)` | `()` | `(_input: float)` | `(_input: Any)` | `new()` | `append(&mut self, input: f64)` |
| `McClellanOscillator` | — | `(advancers, decliners)` | `()` | `(advancers: float, decliners: float)` | `(advancers: Any, decliners: Any)` | `new()` | `append(&mut self, advancers: f64, decliners: f64)` |
| `McClellanSummationIndex` | — | `(advancers, decliners)` | `()` | `(advancers: float, decliners: float)` | `(advancers: Any, decliners: Any)` | `new()` | `append(&mut self, advancers: f64, decliners: f64)` |
| `McGinleyDynamic` | — | `(close)` | `(length: int = 10, c: float = 0.6)` | `(close: float)` | `(close: Any)` | `new(length: usize, c: f64)` | `append(&mut self, close: f64)` |
| `MedianChannel` | — | `(prices)` | `(period: int = 20, multiplier: float = 2.0)` | `(price: float)` | `(prices: Any)` | `new(period: usize, multiplier: f64)` | `append(&mut self, x: f64)` |
| `MedianPrice` | MEDPRICE | `(high, low)` | `()` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new()` | `append(&mut self, high: f64, low: f64)` |
| `MesaAdaptiveMovingAverage` | MAMA | `(_input)` | `(fastlimit: float = 0.5, slowlimit: float = 0.05)` | `(_input: float)` | `(_input: Any)` | `new(fast_limit: f64, slow_limit: f64)` | `append(&mut self, input: f64)` |
| `MinusDirectionalIndicator` | MINUS_DI | `(high, low, close)` | `(timeperiod: int = 14)` | `(h: float, l: float, c: float)` | `(high: Any, low: Any, close: Any)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `MinusDirectionalMovement` | MINUS_DM | `(high, low)` | `(timeperiod: int = 14)` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new(p: usize)` | `append(&mut self, h: f64, l: f64)` |
| `Momentum` | MOM | `(values)` | `(timeperiod: int = 14)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `MoneyFlowIndex` | MFI | `(high, low, close, volume)` | `(timeperiod: int = 14)` | `(high: float, low: float, close: float, volume: float)` | `(high: Any, low: Any, close: Any, volume: Any)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64, close: f64, volume: f64)` |
| `MovingAverage` | MA | `(values)` | `(timeperiod: int = 30, matype: int = 0)` | `(value: float)` | `(values: Any)` | `new(period: usize, ma_type: MaType)` | `append(&mut self, input: f64)` |
| `MovingAverageConvergenceDivergence` | MACD | `(values)` | `(fast_period: int = 12, slow_period: int = 26, signal_period: int = 9)` | `(value: float)` | `(values: Any)` | `new(fast_period: usize, slow_period: usize, signal_period: usize)` | `append(&mut self, input: f64)` |
| `MovingAverageConvergenceDivergenceExtended` | MACDEXT | `(values)` | `(fast_period: int = 12, fast_average_type: int = 1, slow_period: int = 26, slow_average_type: int = 1, signal_period: int = 9, signal_average_type: int = 1)` | `(value: float)` | `(values: Any)` | `new(fastperiod: usize, fastmatype: MaType, slowperiod: usize, slowmatype: MaType, signalperiod: usize, signalmatype: MaType)` | `append(&mut self, input: f64)` |
| `MovingAverageConvergenceDivergenceFixed` | MACDFIX | `(values)` | `(signal_period: int = 9)` | `(value: float)` | `(values: Any)` | `new(signal_period: usize)` | `append(&mut self, input: f64)` |
| `MovingAverageEnvelope` | — | `(values)` | `(period: int = 20, percent: float = 0.025)` | `(value: float)` | `(values: Any)` | `new(period: usize, percent: f64)` | `append(&mut self, input: f64)` |
| `NegativeVolumeIndex` | — | `(close, volume)` | `()` | `(close: float, volume: float)` | `(close: Any, volume: Any)` | `new()` | `append(&mut self, close: f64, volume: f64)` |
| `NewHighsNewLows` | — | `(new_highs, new_lows)` | `()` | `(new_highs: float, new_lows: float)` | `(new_highs: Any, new_lows: Any)` | `new()` | `append(&mut self, new_highs: f64, new_lows: f64)` |
| `NormalizedAverageTrueRange` | NATR | `(high, low, close)` | `(timeperiod: int = 14)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `OnBalanceVolume` | OBV | `(close, volume)` | `()` | `(close: float, volume: float)` | `(close: Any, volume: Any)` | `new()` | `append(&mut self, close: f64, volume: f64)` |
| `OpeningRange` | — | `(high, low, close, anchor)` | `(bars: int = 30)` | `(high: float, low: float, close: float, anchor: bool)` | `(high: Any, low: Any, close: Any, anchor: Any)` | `new(bars: usize)` | `append(&mut self, high: f64, low: f64, close: f64, anchor: bool)` |
| `OrderBlock` | — | `(high, low, close, volume)` | `(swing_length: int = 50, internal_length: int = 5, atr_period: int = 200, threshold: float = 2.0)` | `(high: float, low: float, close: float, volume: float)` | `(high: Any, low: Any, close: Any, volume: Any)` | `new(swing_length: usize, internal_length: usize, atr_period: usize, threshold: f64)` | `append(&mut self, high: f64, low: f64, close: f64, volume: f64)` |
| `OrnsteinUhlenbeckHalfLife` | — | `(price)` | `(timeperiod: int = 20)` | `(price: float)` | `(price: Any)` | `new(timeperiod: usize)` | `append(&mut self, price: f64)` |
| `OutsideBar` | — | `(high, low)` | `()` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new()` | `append(&mut self, high: f64, low: f64)` |
| `OvernightGap` | — | `(open, high, low, close, volume, timestamp)` | `(utc_offset_minutes: int = 0)` | `(open: float, high: float, low: float, close: float, volume: float, timestamp: int)` | `(open: Any, high: Any, low: Any, close: Any, volume: Any, timestamp: Any)` | `new(utc_offset_minutes: i32)` | `append(&mut self, open: f64, _high: f64, _low: f64, close: f64, _volume: f64, timestamp: i64)` |
| `OvernightIntradayReturn` | — | `(open, high, low, close, volume, timestamp)` | `(utc_offset_minutes: int = 0)` | `(open: float, high: float, low: float, close: float, volume: float, timestamp: int)` | `(open: Any, high: Any, low: Any, close: Any, volume: Any, timestamp: Any)` | `new(utc_offset_minutes: i32)` | `append(&mut self, open: f64, _high: f64, _low: f64, close: f64, _volume: f64, timestamp: i64)` |
| `ParabolicMovingAverageStop` | — | `(high, low, close)` | `(length: int = 10, multiplier: float = 3.0)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(period: usize, multiplier: f64)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `ParabolicSar` | SAR | `(high, low)` | `(acceleration: float = 0.02, maximum: float = 0.2)` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new(acceleration: f64, maximum: f64)` | `append(&mut self, high: f64, low: f64)` |
| `ParabolicSarExtended` | SAREXT | `(high, low)` | `(start_value: object = 0.0, offset_on_reverse: object = 0.0, acceleration_init_long: object = 0.02, acceleration_long: object = 0.02, acceleration_max_long: object = 0.2, acceleration_init_short: object = 0.02, acceleration_short: object = 0.02, acceleration_max_short: object = 0.2)` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new(start_value: f64, offset_on_reverse: f64, acceleration_init_long: f64, acceleration_long: f64, acceleration_max_long: f64, acceleration_init_short: f64, acceleration_short: f64, acceleration_max_short: f64)` | `append(&mut self, high: f64, low: f64)` |
| `Parkinson` | — | `(high, low)` | `(timeperiod: int = 20)` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new(timeperiod: usize)` | `append(&mut self, high: f64, low: f64)` |
| `PercentAboveMovingAverage` | — | `(above_moving_average_count, universe_size)` | `()` | `(above_moving_average_count: float, universe_size: float)` | `(above_moving_average_count: Any, universe_size: Any)` | `new()` | `append(&mut self, above_moving_average_count: f64, universe_size: f64)` |
| `PercentagePriceOscillator` | PPO | `(_input)` | `(fastperiod: int = 12, slowperiod: int = 26, moving_average_type: int = 0)` | `(_input: float)` | `(_input: Any)` | `new(fast_period: usize, slow_period: usize, ma_type: MaType)` | `append(&mut self, input: f64)` |
| `PivotPoints` | — | `(high, low, close, anchor)` | `()` | `(high: float, low: float, close: float, anchor: bool)` | `(high: Any, low: Any, close: Any, anchor: Any)` | `new()` | `append(&mut self, high: f64, low: f64, close: f64, anchor: bool)` |
| `PlusDirectionalIndicator` | PLUS_DI | `(high, low, close)` | `(timeperiod: int = 14)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `PlusDirectionalMovement` | PLUS_DM | `(high, low)` | `(timeperiod: int = 14)` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new(period: usize)` | `append(&mut self, h: f64, l: f64)` |
| `PositionHold` | — | `(position)` | `()` | `(position: float)` | `(position: Any)` | `new()` | `append(&mut self, input: f64)` |
| `PositiveVolumeIndex` | — | `(close, volume)` | `()` | `(close: float, volume: float)` | `(close: Any, volume: Any)` | `new()` | `append(&mut self, close: f64, volume: f64)` |
| `PremiumDiscount` | — | `(close)` | `(window: int = 20)` | `(close: float)` | `(close: Any)` | `new(period: usize)` | `append(&mut self, value: f64)` |
| `PreviousHighLow` | — | `(new_session, high, low)` | `()` | `(new_session: bool, high: float, low: float)` | `(new_session: Any, high: Any, low: Any)` | `new()` | `append(&mut self, new_session: bool, high: f64, low: f64)` |
| `ProjectionBands` | — | `(values)` | `(period: int = 20)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, x: f64)` |
| `QuartileBands` | — | `(prices)` | `(period: int = 20)` | `(price: float)` | `(prices: Any)` | `new(period: usize)` | `append(&mut self, x: f64)` |
| `RateOfChange` | ROC | `(values)` | `(timeperiod: int = 14)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RateOfChangePercent` | ROCP | `(values)` | `(timeperiod: int = 14)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RateOfChangeRatio` | ROCR | `(values)` | `(timeperiod: int = 14)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RateOfChangeRatioPercent` | ROCR100 | `(values)` | `(timeperiod: int = 14)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RectangleRange` | — | `(open, high, low, close)` | `()` | `(open: float, high: float, low: float, close: float)` | `(open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, _open: f64, high: f64, low: f64, _close: f64)` |
| `RelativeMomentumIndex` | — | `(close)` | `(timeperiod: int = 14, momentum: int = 5)` | `(close: float)` | `(close: Any)` | `new(period: usize, momentum: usize)` | `append(&mut self, input: f64)` |
| `RelativeStrengthIndex` | RSI | `(close)` | `(timeperiod: int = 14)` | `(close: float)` | `(close: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `Retracements` | — | `(high, low, close)` | `(swing_length: int = 5)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(swing_length: usize)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `Rising` | — | `(_input)` | `(timeperiod: int = 1)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RogersSatchell` | — | `(_open, high, low, close)` | `(timeperiod: int = 20)` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new(timeperiod: usize)` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `RollSpread` | — | `(price)` | `(timeperiod: int = 20)` | `(price: float)` | `(price: Any)` | `new(timeperiod: usize)` | `append(&mut self, price: f64)` |
| `RollingAlpha` | — | `(_input, benchmark)` | `(timeperiod: int = 20)` | `(_input: float, benchmark: float)` | `(_input: Any, benchmark: Any)` | `new(period: usize)` | `append(&mut self, input: f64, benchmark: f64)` |
| `RollingAutocorr` | — | `(_input)` | `(timeperiod: int = 20)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingAverageDeviation` | AVGDEV | `(values)` | `(timeperiod: int = 14)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingAverageDrawdown` | — | `(values)` | `(period: int = 14)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingBeta` | BETA | `(input0, input1)` | `(timeperiod: int = 5)` | `(input0: float, input1: float)` | `(input0: Any, input1: Any)` | `new(period: usize)` | `append(&mut self, input0: f64, input1: f64)` |
| `RollingBetaNeutralSpread` | — | `(x, y)` | `(period: int = 20)` | `(x: float, y: float)` | `(x: Any, y: Any)` | `new(period: usize)` | `append(&mut self, x: f64, y: f64)` |
| `RollingCalmar` | — | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `RollingCoefficientOfDetermination` | — | `(x, y)` | `(period: int = 20)` | `(x: float, y: float)` | `(x: Any, y: Any)` | `new(period: usize)` | `append(&mut self, x: f64, y: f64)` |
| `RollingCointegration` | — | `(left, right)` | `(period: int = 30, augmented_dickey_fuller_lags: int = 1)` | `(left: float, right: float)` | `(left: Any, right: Any)` | `new(period: usize, augmented_dickey_fuller_lags: usize)` | `append(&mut self, left: f64, right: f64)` |
| `RollingConditionalValueAtRisk` | — | `(values)` | `(timeperiod: int = 14, confidence: float = 0.95)` | `(value: float)` | `(values: Any)` | `new(timeperiod: usize, confidence: f64)` | `append(&mut self, input: f64)` |
| `RollingCorrelation` | CORREL | `(input0, input1)` | `(timeperiod: int = 5)` | `(input0: float, input1: float)` | `(input0: Any, input1: Any)` | `new(period: usize)` | `append(&mut self, x: f64, y: f64)` |
| `RollingCovariance` | — | `(left, right)` | `(timeperiod: int = 14)` | `(left: float, right: float)` | `(left: Any, right: Any)` | `new(period: usize)` | `append(&mut self, left: f64, right: f64)` |
| `RollingDrawdownDuration` | — | `(values)` | `()` | `(value: float)` | `(values: Any)` | `new()` | `append(&mut self, input: f64)` |
| `RollingEntropy` | — | `(_input)` | `(timeperiod: int = 20)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingGainLossRatio` | — | `(values)` | `(period: int = 14)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingGrangerCausality` | — | `(dependent, predictor)` | `(period: int = 60, lag: int = 1)` | `(dependent: float, predictor: float)` | `(dependent: Any, predictor: Any)` | `new(period: usize, lag: usize)` | `append(&mut self, dependent: f64, predictor: f64)` |
| `RollingInformationRatio` | — | `(_input, benchmark)` | `(timeperiod: int = 20)` | `(_input: float, benchmark: float)` | `(_input: Any, benchmark: Any)` | `new(period: usize)` | `append(&mut self, input: f64, benchmark: f64)` |
| `RollingInterquartileRange` | — | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `RollingKellyCriterion` | — | `(values)` | `(timeperiod: int = 14)` | `(value: float)` | `(values: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `RollingKendallRankCorrelation` | — | `(x, y)` | `(period: int = 20)` | `(x: float, y: float)` | `(x: Any, y: Any)` | `new(period: usize)` | `append(&mut self, x: f64, y: f64)` |
| `RollingKurtosis` | — | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `RollingLeadLagCrossCorrelation` | — | `(left, right)` | `(window: int = 20, max_lag: int = 10)` | `(left: float, right: float)` | `(left: Any, right: Any)` | `new(window: usize, max_lag: usize)` | `append(&mut self, left: f64, right: f64)` |
| `RollingLinearRegression` | LINEARREG | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingLinearRegressionAngle` | LINEARREG_ANGLE | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingLinearRegressionIntercept` | LINEARREG_INTERCEPT | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingLinearRegressionSlope` | LINEARREG_SLOPE | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingMaximum` | MAX | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingMaximumDrawdown` | — | `(equity)` | `(timeperiod: int = 14)` | `(equity: float)` | `(equity: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingMaximumIndex` | MAXINDEX | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingMedian` | — | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `RollingMedianAbsoluteDeviation` | — | `(values)` | `(period: int = 20)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingMidpoint` | MIDPOINT | `(values)` | `(timeperiod: int = 14)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingMidprice` | MIDPRICE | `(high, low)` | `(timeperiod: int = 14)` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64)` |
| `RollingMinMax` | MINMAX | `(_input)` | `(timeperiod: int = 30)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingMinMaxIndex` | MINMAXINDEX | `(_input)` | `(timeperiod: int = 30)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingMinimum` | MIN | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingMinimumIndex` | MININDEX | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingMode` | — | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `RollingOmegaRatio` | — | `(values)` | `(timeperiod: int = 14, threshold: float = 0.0)` | `(value: float)` | `(values: Any)` | `new(timeperiod: usize, threshold: f64)` | `append(&mut self, input: f64)` |
| `RollingPainIndex` | — | `(values)` | `(period: int = 14)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingPairwiseBeta` | — | `(asset, benchmark)` | `(period: int = 20)` | `(asset: float, benchmark: float)` | `(asset: Any, benchmark: Any)` | `new(period: usize)` | `append(&mut self, asset: f64, benchmark: f64)` |
| `RollingPercentile` | — | `(_input)` | `(timeperiod: int = 14, percentile: float = 50.0)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize, percentile: f64)` | `append(&mut self, input: f64)` |
| `RollingProfitFactor` | — | `(values)` | `(timeperiod: int = 14)` | `(value: float)` | `(values: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `RollingQuantile` | — | `(_input)` | `(timeperiod: int = 14, quantile: float = 0.5)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize, quantile: f64)` | `append(&mut self, input: f64)` |
| `RollingRank` | — | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `RollingRecoveryFactor` | — | `(values)` | `(timeperiod: int = 14)` | `(value: float)` | `(values: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `RollingSharpe` | — | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `RollingSkew` | — | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `RollingSortino` | — | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `RollingSpearmanCorrelation` | — | `(x, y)` | `(period: int = 20)` | `(x: float, y: float)` | `(x: Any, y: Any)` | `new(period: usize)` | `append(&mut self, x: f64, y: f64)` |
| `RollingStandardDeviation` | STDDEV | `(values)` | `(timeperiod: int = 14, nbdev: float = 1.0)` | `(value: float)` | `(values: Any)` | `new(period: usize, nbdev: f64)` | `append(&mut self, input: f64)` |
| `RollingStandardError` | — | `(values)` | `(period: int = 20)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingSum` | SUM | `(values)` | `(timeperiod: int = 30)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingTimeSeriesForecast` | TSF | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `RollingTreynorRatio` | — | `(values, benchmark)` | `(timeperiod: int = 14)` | `(value: float, benchmark: float)` | `(values: Any, benchmark: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64, benchmark: f64)` |
| `RollingValueAtRisk` | — | `(values)` | `(timeperiod: int = 14, confidence: float = 0.95)` | `(value: float)` | `(values: Any)` | `new(timeperiod: usize, confidence: f64)` | `append(&mut self, input: f64)` |
| `RollingVariance` | VAR | `(values)` | `(timeperiod: int = 14, nbdev: float = 1.0)` | `(value: float)` | `(values: Any)` | `new(period: usize, _nbdev: f64)` | `append(&mut self, input: f64)` |
| `RollingVarianceRatio` | — | `(a, b)` | `(period: int = 60, q: int = 2)` | `(a: float, b: float)` | `(a: Any, b: Any)` | `new(period: usize, q: usize)` | `append(&mut self, a: f64, b: f64)` |
| `RollingVolumeWeightedAveragePrice` | — | `(high, low, close, volume)` | `(timeperiod: int = 20)` | `(high: float, low: float, close: float, volume: float)` | `(high: Any, low: Any, close: Any, volume: Any)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64, close: f64, volume: f64)` |
| `RollingWinsorize` | — | `(_input)` | `(timeperiod: int = 14, lower: float = 0.05, upper: float = 0.95)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize, lower: f64, upper: f64)` | `append(&mut self, input: f64)` |
| `RollingZScore` | — | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `RoofingFilter` | — | `(values)` | `(low_period: int = 10, high_period: int = 48)` | `(value: float)` | `(values: Any)` | `new(low_period: usize, high_period: usize)` | `append(&mut self, input: f64)` |
| `SchaffTrendCycle` | — | `(close)` | `(tclength: int = 10, fast: int = 12, slow: int = 26, factor: float = 0.5)` | `(close: float)` | `(close: Any)` | `new(tclength: usize, fast: usize, slow: usize, factor: f64)` | `append(&mut self, close: f64)` |
| `SessionExtrema` | — | `(new_session, high, low)` | `()` | `(new_session: bool, high: float, low: float)` | `(new_session: Any, high: Any, low: Any)` | `new()` | `append(&mut self, new_session: bool, high: f64, low: f64)` |
| `SessionRange` | — | `(open, high, low, close, volume, timestamp)` | `(utc_offset_minutes: int = 0)` | `(open: float, high: float, low: float, close: float, volume: float, timestamp: int)` | `(open: Any, high: Any, low: Any, close: Any, volume: Any, timestamp: Any)` | `new(utc_offset_minutes: i32)` | `append(&mut self, high: f64, low: f64)` |
| `SessionVolumeLevels` | — | `(high, low, close, volume, anchor)` | `(bins: int = 24, value_area: float = 0.7)` | `(high: float, low: float, close: float, volume: float, anchor: bool)` | `(high: Any, low: Any, close: Any, volume: Any, anchor: Any)` | `new(bins: usize, value_area: f64)` | `append(&mut self, high: f64, low: f64, close: f64, volume: f64, anchor: bool)` |
| `SessionVolumeWeightedAveragePrice` | — | `(open, high, low, close, volume, timestamp)` | `(utc_offset_minutes: int = 0)` | `(open: float, high: float, low: float, close: float, volume: float, timestamp: int)` | `(open: Any, high: Any, low: Any, close: Any, volume: Any, timestamp: Any)` | `new(utc_offset_minutes: i32)` | `append(&mut self, _open: f64, high: f64, low: f64, close: f64, volume: f64, timestamp: i64)` |
| `Sessions` | — | `(new_session, high, low)` | `()` | `(new_session: bool, high: float, low: float)` | `(new_session: Any, high: Any, low: Any)` | `new()` | `append(&mut self, new_session: bool, high: f64, low: f64)` |
| `SharkPattern` | — | `(open, high, low, close)` | `()` | `(open: float, high: float, low: float, close: float)` | `(open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, _open: f64, high: f64, low: f64, _close: f64)` |
| `SignalDelay` | — | `(_input)` | `(timeperiod: int = 1)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `SignedPower` | — | `(input)` | `(exponent: float = 2.0)` | `(input: float)` | `(input: Any)` | `new(exponent: f64)` | `append(&mut self, input: f64)` |
| `SimpleMovingAverage` | SMA | `(values)` | `(timeperiod: int = 30)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `SmoothedTrendChannel` | — | `(high, low, close)` | `(length: int = 10)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `SpreadZScore` | — | `(x, y)` | `(timeperiod: int = 20)` | `(x: float, y: float)` | `(x: Any, y: Any)` | `new(timeperiod: usize)` | `append(&mut self, x: f64, y: f64)` |
| `Squeeze` | — | `(high, low, close)` | `(bb_length: int = 20, bb_std: float = 2.0, kc_length: int = 20, kc_scalar: float = 1.5, mom_length: int = 12, mom_smooth: int = 6)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(bb_length: usize, bb_std: f64, kc_length: usize, kc_scalar: f64, mom_length: usize, mom_smooth: usize)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `SqueezePro` | — | `(high, low, close)` | `(bb_length: int = 20, bb_std: float = 2.0, kc_length: int = 20, kc_scalar_wide: float = 2.0, kc_scalar_normal: float = 1.5, kc_scalar_narrow: float = 1.0, mom_length: int = 12, mom_smooth: int = 6)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(bb_length: usize, bb_std: f64, kc_length: usize, kc_scalar_wide: f64, kc_scalar_normal: f64, kc_scalar_narrow: f64, mom_length: usize, mom_smooth: usize)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `StandardErrorBands` | — | `(values)` | `(period: int = 21, multiplier: float = 2.0)` | `(value: float)` | `(values: Any)` | `new(period: usize, multiplier: f64)` | `append(&mut self, input: f64)` |
| `StochasticOscillator` | STOCH | `(high, low, close)` | `(fast_k_period: int = 5, slow_k_period: int = 3, slow_k_average_type: int = 0, slow_d_period: int = 3, slow_d_average_type: int = 0)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(fastk_period: usize, slowk_period: usize, slowk_matype: MaType, slowd_period: usize, slowd_matype: MaType)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `StochasticRelativeStrengthIndex` | STOCHRSI | `(_input)` | `(time_period: int = 14, fast_k_period: int = 5, fast_d_period: int = 3, fast_d_average_type: int = 0)` | `(_input: float)` | `(_input: Any)` | `new(timeperiod: usize, fastk_period: usize, fastd_period: usize, fastd_matype: MaType)` | `append(&mut self, input: f64)` |
| `SuperSmoother` | — | `(values)` | `(period: int = 10)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `Supertrend` | — | `(high, low, close)` | `(timeperiod: int = 7, multiplier: float = 3.0)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(period: usize, multiplier: f64)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `SwingHighLow` | — | `(high, low)` | `(swing_length: int = 5)` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new(length: usize)` | `append(&mut self, high: f64, low: f64)` |
| `ThreeDrives` | — | `(open, high, low, close)` | `()` | `(open: float, high: float, low: float, close: float)` | `(open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, _open: f64, high: f64, low: f64, _close: f64)` |
| `TimeOfDayReturnProfile` | — | `(open, high, low, close, volume, timestamp)` | `(buckets: int = 24, utc_offset_minutes: int = 0)` | `(open: float, high: float, low: float, close: float, volume: float, timestamp: int)` | `(open: Any, high: Any, low: Any, close: Any, volume: Any, timestamp: Any)` | `new(buckets: usize, utc_offset_minutes: i32)` | `append(&mut self, _open: f64, _high: f64, _low: f64, close: f64, _volume: f64, timestamp: i64)` |
| `TimeSegmentedVolume` | — | `(close, volume)` | `(period: int = 18)` | `(close: float, volume: float)` | `(close: Any, volume: Any)` | `new(period: usize)` | `append(&mut self, close: f64, volume: f64)` |
| `TimeSeriesRank` | — | `(input)` | `(timeperiod: int = 14)` | `(input: float)` | `(input: Any)` | `new(timeperiod: usize)` | `append(&mut self, input: f64)` |
| `TomDeMarkSequential` | — | `(close)` | `()` | `(close: float)` | `(close: Any)` | `new()` | `append(&mut self, close: f64)` |
| `TradeVolumeIndex` | — | `(close, volume)` | `(min_tick: float = 0.25)` | `(close: float, volume: float)` | `(close: Any, volume: Any)` | `new(min_tick: f64)` | `append(&mut self, close: f64, volume: f64)` |
| `TrianglePattern` | — | `(open, high, low, close)` | `()` | `(open: float, high: float, low: float, close: float)` | `(open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, _open: f64, high: f64, low: f64, _close: f64)` |
| `TriangularMovingAverage` | TRIMA | `(values)` | `(timeperiod: int = 30)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `TripleExponentialAverage` | T3 | `(values)` | `(timeperiod: int = 5, volume_factor: float = 0.7)` | `(value: float)` | `(values: Any)` | `new(period: usize, v_factor: f64)` | `append(&mut self, input: f64)` |
| `TripleExponentialMovingAverage` | TEMA | `(values)` | `(timeperiod: int = 30)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `TripleExponentialRateOfChange` | TRIX | `(_input)` | `(timeperiod: int = 30)` | `(value: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `TripleTopBottom` | — | `(open, high, low, close)` | `()` | `(open: float, high: float, low: float, close: float)` | `(open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, _open: f64, high: f64, low: f64, _close: f64)` |
| `TrueRange` | TRANGE | `(high, low, close)` | `()` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `TrueStrengthIndex` | — | `(_input)` | `(fast: int = 13, slow: int = 25)` | `(_input: float)` | `(_input: Any)` | `new(fast: usize, slow: usize)` | `append(&mut self, input: f64)` |
| `TwiggsMoneyFlow` | — | `(high, low, close, volume)` | `(period: int = 21)` | `(high: float, low: float, close: float, volume: float)` | `(high: Any, low: Any, close: Any, volume: Any)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64, close: f64, volume: f64)` |
| `TypicalPrice` | TYPPRICE | `(high, low, close)` | `()` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `UlcerIndex` | — | `(_input)` | `(timeperiod: int = 14)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `UltimateOscillator` | ULTOSC | `(high, low, close)` | `(timeperiod1: int = 7, timeperiod2: int = 14, timeperiod3: int = 28)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(timeperiod1: usize, timeperiod2: usize, timeperiod3: usize)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `UpDownVolumeRatio` | — | `(advancing_volume, declining_volume)` | `()` | `(advancing_volume: float, declining_volume: float)` | `(advancing_volume: Any, declining_volume: Any)` | `new()` | `append(&mut self, advancing_volume: f64, declining_volume: f64)` |
| `ValueWhen` | — | `(condition, _input)` | `()` | `(condition: bool, _input: float)` | `(condition: Any, _input: Any)` | `new()` | `append(&mut self, condition: bool, input: f64)` |
| `VariableIndexDynamicAverage` | — | `(close)` | `(length: int = 14, cmo_period: int = 9, alpha: float \| None = None)` | `(close: float)` | `(close: Any)` | `new(length: usize, cmo_period: usize, alpha: f64)` | `append(&mut self, input: f64)` |
| `VariablePeriodMovingAverage` | MAVP | `(values, periods)` | `(min_period: int = 2, max_period: int = 30, average_type: int = 0)` | `(value: float, period: float)` | `(values: Any, periods: Any)` | `new(min_period: usize, max_period: usize, average_type: MaType)` | `append(&mut self, input: f64, period: f64)` |
| `VolumeByTimeProfile` | — | `(open, high, low, close, volume, timestamp)` | `(buckets: int = 24, utc_offset_minutes: int = 0)` | `(open: float, high: float, low: float, close: float, volume: float, timestamp: int)` | `(open: Any, high: Any, low: Any, close: Any, volume: Any, timestamp: Any)` | `new(buckets: usize, utc_offset_minutes: i32)` | `append(&mut self, _open: f64, _high: f64, _low: f64, _close: f64, volume: f64, timestamp: i64)` |
| `VolumeOscillator` | — | `(volume)` | `(fast: int = 5, slow: int = 10)` | `(volume: float)` | `(volume: Any)` | `new(fast: usize, slow: usize)` | `append(&mut self, volume: f64)` |
| `VolumePriceTrend` | — | `(close, volume)` | `()` | `(close: float, volume: float)` | `(close: Any, volume: Any)` | `new()` | `append(&mut self, close: f64, volume: f64)` |
| `VolumeRelativeStrengthIndex` | — | `(volume)` | `(period: int = 14)` | `(volume: float)` | `(volume: Any)` | `new(period: usize)` | `append(&mut self, volume: f64)` |
| `VolumeWeightedMovingAverage` | — | `(price, volume)` | `(timeperiod: int = 10)` | `(price: float, volume: float)` | `(price: Any, volume: Any)` | `new(period: usize)` | `append(&mut self, price: f64, volume: f64)` |
| `VolumeWeightedMovingAverageConvergenceDivergence` | — | `(close, volume)` | `(fast: int = 12, slow: int = 26, signal: int = 9)` | `(close: float, volume: float)` | `(close: Any, volume: Any)` | `new(fast: usize, slow: usize, signal: usize)` | `append(&mut self, close: f64, volume: f64)` |
| `VolumeZoneOscillator` | — | `(close, volume)` | `(timeperiod: int = 14)` | `(close: float, volume: float)` | `(close: Any, volume: Any)` | `new(timeperiod: usize)` | `append(&mut self, close: f64, volume: f64)` |
| `Vortex` | — | `(high, low, close)` | `(window: int = 14)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `WedgePattern` | — | `(open, high, low, close)` | `()` | `(open: float, high: float, low: float, close: float)` | `(open: Any, high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, _open: f64, high: f64, low: f64, _close: f64)` |
| `WeightedClose` | WCLPRICE | `(high, low, close)` | `()` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `WeightedMovingAverage` | WMA | `(values)` | `(timeperiod: int = 30)` | `(value: float)` | `(values: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `WilliamsAccumulationDistribution` | — | `(high, low, close)` | `()` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new()` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `WilliamsPercentR` | WILLR | `(high, low, close)` | `(timeperiod: int = 14)` | `(high: float, low: float, close: float)` | `(high: Any, low: Any, close: Any)` | `new(period: usize)` | `append(&mut self, high: f64, low: f64, close: f64)` |
| `YangZhang` | — | `(_open, high, low, close)` | `(timeperiod: int = 20)` | `(_open: float, high: float, low: float, close: float)` | `(_open: Any, high: Any, low: Any, close: Any)` | `new(timeperiod: usize)` | `append(&mut self, open: f64, high: f64, low: f64, close: f64)` |
| `ZeroLagExponentialMovingAverage` | — | `(_input)` | `(timeperiod: int = 10)` | `(_input: float)` | `(_input: Any)` | `new(period: usize)` | `append(&mut self, input: f64)` |
| `ZigZag` | — | `(high, low)` | `(threshold: float = 0.05)` | `(high: float, low: float)` | `(high: Any, low: Any)` | `new(threshold: f64)` | `append(&mut self, high: f64, low: f64)` |
